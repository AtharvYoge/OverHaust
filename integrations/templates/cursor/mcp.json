{
  "mcpServers": {
    "overhaust": {
      "command": "{{PYTHON}}",
      "args": ["-m", "services.mcp_server.server"],
      "cwd": "{{REPO_ROOT}}",
      "env": {
        "PYTHONPATH": "{{REPO_ROOT}}",
        "OVERHAUST_ROOT": "{{REPO_ROOT}}"
      }
    }
  }
}
