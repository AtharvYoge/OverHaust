{
  "name": "Overhaust",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "backend-api",
      "command": "python3 -m services.api.main",
      "description": "Overhaust FastAPI backend, serving on http://localhost:8000 (docs at /docs)."
    },
    {
      "name": "frontend-web",
      "command": "cd apps/web && VITE_API_BASE_URL=http://localhost:8000 npm run dev -- --host",
      "description": "Overhaust React + Vite dev server, serving on http://localhost:5173."
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 },
    { "name": "web", "port": 5173 }
  ]
}
