[package]
name = "overhaust-desktop"
version = "0.1.0"
description = "OverHaust desktop shell"
authors = ["Peace Labs Creative"]
edition = "2021"

[lib]
name = "overhaust_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
